import { RefObject, createContext } from "react";
//import { GlobalState } from "./GlobalProvider";

/*export type GlobalContextProps = {
    viewerC:GlobalState,
}*/

export const GlobalContext = createContext({} as any);